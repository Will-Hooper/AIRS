name: Update O-NET Data Monthly
on: workflow_dispatch

permissions:
  contents: write

jobs:
  update-onet:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - shell: pwsh
        run: ./backend/onet_sync.ps1 -Force
